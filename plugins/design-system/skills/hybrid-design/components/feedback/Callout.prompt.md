Inline message banner — note/warn/error/success block with a status glyph and left accent bar.

```jsx
<Callout tone="warning" title="Heads up">This branch is behind main.</Callout>
```

Tones: `info | success | warning | danger`. Optional `title` line above the body.
