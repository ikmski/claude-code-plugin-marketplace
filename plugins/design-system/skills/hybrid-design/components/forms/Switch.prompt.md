Binary on/off toggle — track fills accent-blue when on, mechanical slide.

```jsx
<Switch checked={on} onChange={setOn} label="Format on save" />
```

`onChange(next)` receives the new boolean. Optional inline `label`.
