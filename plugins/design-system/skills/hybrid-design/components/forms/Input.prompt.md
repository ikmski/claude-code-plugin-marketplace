Single-line text field — monospace, sunken well, blue focus ring with tint glow.

```jsx
<Input label="Repository" placeholder="org/name" prefix={<Icon/>} />
<Input error value="bad value" />
```

Props: `label`, `prefix`/`suffix` adornments, `error` (red border), `disabled`. Uppercase caps-tracked label sits above.
