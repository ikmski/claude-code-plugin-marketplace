Square checkbox with accent fill. Supports an indeterminate (mixed) state.

```jsx
<Checkbox checked={a} onChange={setA} label="Run on push" />
<Checkbox indeterminate label="Select all" />
```

`onChange(next)` receives the new boolean.
