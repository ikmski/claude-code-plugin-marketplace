Compact status/label chip — use for states, counts, tags on a row; keyed to the syntax palette.

```jsx
<Badge tone="green" dot>passing</Badge>
<Badge tone="yellow">deprecated</Badge>
```

Tones: `neutral | blue | green | yellow | red | cyan | purple`. `dot` adds a leading status dot. Monospace, uppercase-friendly, soft tint fill.
