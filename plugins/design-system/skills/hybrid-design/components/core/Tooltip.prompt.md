Hover/focus hint on a raised surface. Wrap any element.

```jsx
<Tooltip content="git pull --rebase" side="top">
  <Button variant="ghost">Sync</Button>
</Tooltip>
```

`side`: top (default) | bottom | left | right.
