Identity chip — image with monospace-initials fallback. Square by default
(on-brand); pass `round` for a circle. Optional status dot.

```jsx
<Avatar name="Ada Lovelace" status="online" />
<Avatar src="/u/ada.png" name="Ada Lovelace" size={40} round />
```
